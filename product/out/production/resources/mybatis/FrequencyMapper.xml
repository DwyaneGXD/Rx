<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.oasis.rx.product.dao.FrequencyDao">

    <select id="findIsRepeat" resultType="com.oasis.rx.product.entity.FrequencyEntity">
        SELECT frequency_name
        FROM t_frequency
        WHERE
        frequency_name = #{arg0} and is_delete = 0;
    </select>

    <delete id="deleteId" parameterType="java.lang.String">
        DELETE FROM t_frequency
        WHERE id =#{arg0}
    </delete>

</mapper>